deliveryCity = "Anytown";
orderPrice = 20;
birthday = "no";


if (((deliveryCity === "Anytown") && (orderPrice > 10)) ||  (birthday === "yes")) {

    var deliveryPrice = 0;
    } else {
    var deliveryPrice = 5;
    }

console.log(deliveryPrice);